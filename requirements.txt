# — paquetes base —
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
apscheduler==3.10.4
requests==2.32.0
nest_asyncio==1.6.0
pybit==5.7.0          # unified_trading incluido
uvloop==0.19.0        # bucle de eventos ultra-rápido
# — TensorFlow CPU only (menos de 200 MB) —
tensorflow-cpu==2.15.1
